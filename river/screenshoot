#!/bin/sh

DIR="$(xdg-user-dir PICTURES)/Screenshot"
FILE=$(date +'%Y%e%d-%s.png')

mkdir -p "$DIR"
grim -g "$(slurp)" -l 3 - | tee "$DIR/$FILE" | wl-copy

if [ -f "$DIR/$FILE" ]; then
    notify-send -i "$DIR/$FILE" -u normal "Screenshot Saved" "$FILE"
else
    notify-send -u normal "Screenshot Failed" "Unable to save screenshot"
fi
